function makeDivs() {
    let row = 5
    for(let b = 0; b < row; b++){
        for(let b = 0; b < row; b++){
            let box = document.createElement("div");
            box.style.margin("1px");
            box.style.backgroundColor("black");
            
        }
    }
}